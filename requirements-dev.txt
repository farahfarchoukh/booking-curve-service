# Everything beyond requirements.txt: the API service, its test suite,
# linting, and re-executing notebooks/exploration.ipynb from scratch.
# Nothing in `src/train.py` / `evaluation/evaluate.py` needs any of this —
# see README "What's implemented" for the split. requirements-api.txt is
# also what the Dockerfile installs into the runtime image — one file,
# not two copies of the same versions to keep in sync by hand.
-r requirements.txt
-r requirements-api.txt
httpx==0.28.1
pytest==9.1.1
pytest-cov==7.1.0
pip-audit==2.10.1
ruff==0.16.6
nbformat==5.11.1
nbclient==0.11.0
ipykernel==7.3.0
matplotlib==3.11.1
seaborn==0.13.2
# was only a transitive scikit-learn dep before this; pinned explicitly now
# that evaluation/price_demand_eda.py imports it directly (Spearman correlation)
scipy==1.18.1
